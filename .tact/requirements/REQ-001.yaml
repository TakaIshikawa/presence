id: REQ-001
title: "Per-commit posts: Every 10 minutes, checks for new commits. Finds
  related Claude Code prompts, synthesizes an X post, evaluates quality, and
  auto-posts if it passes threshold."
approval: approved
phase: planned
priority: high
description: "Implement: Per-commit posts: Every 10 minutes, checks for new
  commits. Finds related Claude Code prompts, synthesizes an X post, evaluates
  quality, and auto-posts if it passes threshold."
acceptanceCriteria:
  - "Per-commit posts: Every 10 minutes, checks for new commits. Finds related
    Claude Code prompts, synthesizes an X post, evaluates quality, and
    auto-posts if it passes threshold."
decomposition:
  assignments: []
dependencies: []
